apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: octy-production
  labels:
    app: messaging-app
spec:
  type: ClusterIP
  selector:
    app: messaging-app
  ports:
    - protocol: TCP
      name: http
      port: 1033
      targetPort: 8080
