apiVersion: v1
kind: Service
metadata:
  name: octy-jobs-service
  namespace: octy-production
  labels:
    app: octy-jobs-app
spec:
  type: ClusterIP
  selector:
    app: octy-jobs-app
  ports:
    - protocol: TCP
      name: http
      port: 1034
      targetPort: 8080
