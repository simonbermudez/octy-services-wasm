apiVersion: v1
kind: Service
metadata:
  name: profiles-service
  namespace: octy-production
  labels:
    app: profiles-app
spec:
  type: ClusterIP
  selector:
    app: profiles-app
  ports:
    - protocol: TCP
      name: http
      port: 1027
      targetPort: 8080
