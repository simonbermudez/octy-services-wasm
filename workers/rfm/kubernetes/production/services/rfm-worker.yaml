apiVersion: v1
kind: Service
metadata:
  name: rfm-worker
  namespace: octy-production
  labels:
    app: rfm-worker-app
spec:
  type: ClusterIP
  selector:
    app: rfm-worker-app
  ports:
    - protocol: TCP
      name: http
      port: 1037
      targetPort: 8080