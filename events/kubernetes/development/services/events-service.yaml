apiVersion: v1
kind: Service
metadata:
  name: events-service
  labels:
    app: events-app
spec:
  type: ClusterIP
  selector:
    app: events-app
  ports:
    - protocol: TCP
      name: http
      port: 1029
      targetPort: 8080
