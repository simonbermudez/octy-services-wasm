---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: segmentation-deploy
  namespace: octy-production
  labels:
    app: segmentation-app
spec:
  selector:
    matchLabels:
      app: segmentation-app
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: segmentation-app
    spec:
      containers:
      - name: segmentation-app
        image: benpayflic/octy-segmentation-service:$COMMIT_SHA1
        imagePullPolicy: Always
        env:
          - name: SEGMENTATION_CONFIG # name that can be refefenced in app
            valueFrom:
              configMapKeyRef:
                name: segmentation-config  # The ConfigMap this value comes from.
                key: segmentation_config # The key to fetch.
          - name: SEGMENTATION_SECRETS # name that can be refefenced in app
            valueFrom:
              secretKeyRef:
                name: segmentation-secrets # The Secret this value comes from.
                key: segmentation_secrets # The key to fetch.
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "300m"
      imagePullSecrets:
      - name: docker-hub-key