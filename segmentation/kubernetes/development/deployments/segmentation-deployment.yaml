---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: segmentation-deploy
  namespace: octy-development
  labels:
    app: segmentation-app
spec:
  selector:
    matchLabels:
      app: segmentation-app
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: segmentation-app
    spec:
      containers:
      - name: segmentation-app
        image: octysonity/octy-segmentation-service:latest
        imagePullPolicy: Always
        # env:
        #   - name: ACCOUNT_CONFIG # name that can be refefenced in app
        #     valueFrom:
        #       configMapKeyRef:
        #         name: account-config  # The ConfigMap this value comes from.
        #         key: account_config # The key to fetch.
        #   - name: ACCOUNT_SECRETS # name that can be refefenced in app
        #     valueFrom:
        #       secretKeyRef:
        #         name: account-secrets # The Secret this value comes from.
        #         key: account_secrets # The key to fetch.
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
      imagePullSecrets:
      - name: docker-hub-key